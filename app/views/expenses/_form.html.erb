<%= form_with(model: expense, local: true) do |form| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <div class="field">
    <%= form.label :ward_id %><br />
    <%= form.collection_select :ward_id, @wards, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :journalDate %>
    <%= form.datetime_select :journalDate, id: :expense_journalDate %>
  </div>

  <div class="field">
    <%= form.label :vendor %>
    <%= form.text_field :vendor, id: :expense_vendor %>
  </div>

  <div class="field">
    <%= form.label :expenseTypes_id %><br />
    <%= form.collection_select :expenseTypes_id, @expTypes, :id, :description %>
  </div>

<!--  <div class="field">
    <%= form.label :expenseType %>
    <%= form.number_field :expenseType, id: :expense_expenseType %>
  </div>-->

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :expense_description %>
  </div>

  <div class="field">
    <%= form.label :account %>
    <%= form.number_field :account, id: :expense_account %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :expense_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
